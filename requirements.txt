# Runtime for the /trade product: Dashboard (GridMind, 8765) + 交易台 trading-desk (8790).
# Everything else the platform needs is the Python standard library.
numpy>=2.0,<3
pandas>=2.2,<4
fastapi>=0.115,<1
uvicorn>=0.30,<1
pydantic>=2,<3
pytest>=8
