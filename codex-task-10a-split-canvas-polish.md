# Codex 任务 10a:双画布纯前端修缮(重叠/比例/文案/相位 gating)

> 背景:task-09(09a/09b)交付的 `dashboard-dualtrack-split.html` 经所有者提出、Claude 用 DOM 实测 + 多分辨率截图完成了一轮全面前端审计。本任务修其中**纯前端、零后端风险**的部分;交易链路(真实下单 payload、价格接入、TP/SL 拖拽)在 task-10b,**本任务不碰任何 POST 逻辑**。
>
> 每一条都有实测证据,不是审美意见。修复以证据为准;规格未覆盖的决策选最保守做法,PR 里列出。

## 硬约束(与 task-09 相同,重申)

1. **盲答协议**:机器轨 `mid` 相位不得出现网格点位/逐单明细;所有改动不许触碰这个边界。
2. **v5 零 diff**:`dashboard-dualtrack-v5.html` 与 `tests/test_dashboard_dualtrack_static.py` 现有断言一字不动。
3. **decision-log.md 追加末尾**;R4 三文件(`pipelines/lab_run.py`/`services/lab_registry.py`/`services/lab_r4_promotion.py`)零 diff。
4. **design-token 纪律**:只用 `assets/tokens.css` 变量;`tests/test_design_tokens_static.py` 全绿。
5. **本任务零后端**:不改 `pipelines/dashboard_server.py` 的任何 API 行为、不改 `services/`(唯一例外:A6 允许改 `packages/standard-kline/` 的 id→class,那是前端包)。

## A1 · 做多/做空按钮与图表工具条重叠(P1,所有者点名)

**证据**:DOM 实测 `.inbtns` 位于 (50,377) 尺寸 100×50,standard-kline 工具条的 +/−/</> 四键位于 x46-163、y371,完全相交;1280px 视口下缩放键被整个盖住只露出 fit。根因:mockup 的假 SVG 没有工具条,`top:var(--sp-3);left:var(--sp-3)` 照搬后撞上真组件。

**修法**(选一,PR 说明):把做多/做空按钮移进工具条同一行的右端(工具条是 flex,追加两个按钮槽);或按钮下移到工具条下方紧贴(`top` 加上工具条实际高度)。**验收标准是可测的**:浏览器里对 `.inbtns` 与 `.standard-kline-toolbar` 及其每个子按钮做 `getBoundingClientRect` 相交检测,交集必须为 0;1280/1440/1600 三档宽度都要测。

## A2 · 主图/大级别图比例重排(P1,所有者点名)

**证据**:1600px 视口下每画布约 750px,主图仅 506px 宽(视口的 32%);大级别图 217px 宽,内部要塞 4 个 tf 选项 + 5 个缩放键 + 归因徽标,工具条换行吃掉约 70px,实际画图区约 190×190px。

**修法**:放弃 mockup 的"主图 70 | 大级别图 30 同一行"(那是按单画布全宽设计的,双画布并排后数学不成立)。改为:
- 主图占画布**全宽**一行。
- 两个大级别图移到主图**下方横排**(各约 50% 画布宽,高度 ~160-180px)。
- 大级别图的工具条精简:小图不需要完整缩放键组,保留 tf-seg(15m/30m/1h/4h)即可;fit 之外的 +/−/</> 在 200px 高的小图上没有操作价值,允许通过 standard-kline 现有配置隐藏(如果包不支持按钮组开关,允许加一个 `toolbar:{zoom:false}` 级别的可选配置,provider-agnostic 命名,包版本 0.2.x 递增,附包内测试)。
- 媒体断点:≥1280px 必须保持双画布两列(当前 1280 就塌成单列了,实测 `canvasCols` 单列;mockup 声明的断点是 1200)。重排后画布最小可用宽度下降,这一条应该自然达成,达不成要在 PR 里说明原因。

## A3 · 机器轨盘中的空黑框收缩(P1)

**证据**:盲测期机器轨 K线图 widget 是 534px 高的空黑框,只有一句"盲测中"居中——注意力黑洞,浪费半个画布。

**修法**:`mid` 相位下机器轨 kline widget 收缩为一条 ~120px 高的盲态条(内容:盲测提示 + 当前已实现/未实现 PnL 两个数字,这两个数字本来就是盲测期允许公布的);`post` 相位(真实收盘后)恢复完整图并揭示。注意:收缩由**真实周期状态**驱动(见 A5),不是页签选择。

## A4 · 文案清理:开发注记、原始 key、语义错误(P2)

1. **删掉所有内部任务编号**,实测至少 4 处:副标题"09a 骨架"(line 120)、K线图 header"TP/SL 覆盖层 09b 接入"(line 146)、成交 header"09a 使用 human 基础状态;逐 order 明细 09b"(line 170)、图内"拖拽 09b"(line 153)。清完后 `grep -c "09a\|09b" dashboard-dualtrack-split.html` 必须为 0(decision-log 与测试文件不受此限)。
2. **机器轨状态 widget**(line 389):删掉 `machine_fills_hidden · false` 这行原始 key。替换为人话:成交明细状态从周期是否收盘推导——盘中显示"成交明细 · 盲测中",收盘后显示"成交明细 · 已揭示"。**不要**直接把 `machine_fills_hidden` 布尔值翻译成中文了事,它是 runtime 采样字段,语义和用户想知道的"现在明细可不可见"并不重合。
3. **人轨状态 widget**:第一行现在只渲染一个单词"warn"。runtime status 必须映射成中文短语 + 对应状态色圆点(run→绿"正常",warn→黄"注意",block→红"阻塞",unknown→灰"未知";具体映射看 runtime/status 响应里的实际取值,以真实响应为准)。
4. **持仓 widget 空仓语义**:当前空仓时显示"做多 —— / TP/SL —— / 4,155.0"——没有仓位却显示方向和 SL(作战单数据混进了持仓)。空仓时整个 widget 主体显示"无持仓"一行即可,不显示方向/均价/TP-SL 骨架。
5. **风险 widget 空仓语义**:空仓时"SL 状态:有效"是语义错误。空仓时保证金占用/最大可亏/强平距离/SL 状态全部显示"无持仓"或 `--`,只保留杠杆行(杠杆是配置,与持仓无关)。
6. **机器轨风险 widget 盘中**:"SL 状态:未设置"与作战单失效价 4155 矛盾(09a 占位符原本写"盲测中",09b 接数后反而退步)。盘中恢复"盲测中",收盘后显示真实值。
7. **收益 widget 口径脚注**:已实现(当前周期)与今日(含今天已收盘周期)数字不同没有任何解释,实测 -$206.98 vs -$263.30。在 widget 底部加一行 `sub` 级说明:"已实现/未实现=本周期 · 今日/一周=mark-to-market 含进行中周期"(措辞可精简,意思必须在)。
8. **下单金额档对齐 mockup**:$1k/$2k/$5k/$8k(当前是 $2k/$4k/$8k)。
9. **页面 title/副标题**:title 保持"双轨画布"系;副标题删除开发注记后写产品语言(如"人轨/机器轨 · 零共享双画布")。全站三套品牌名(Goldbot/Trading Orchestrator/黄金交易后台)的统一是所有者级决策,**不在本任务范围**,PR 里列为未决事项即可。

## A5 · 相位页签接上真实周期状态(P3,结构性)

**证据**:页签是纯 UI 切换(`data-phase-target` 只改 `data-phase` 属性)。盘中可以点"盘前"看到可点的"锁定作战单"按钮(POST 发出去 400 被吞);点"盘后复盘"看到满屏 `--` 的复盘 widget。

**修法**:
1. **真实相位推导**:从 `state.cycle` 推导当前真实相位(周期未开始=pre / 进行中=mid / 已收盘=post;"已收盘"复用现有 `cycleClosed()`,不要造第二套)。页面加载与轮询刷新时,默认页签自动跟随真实相位。
2. **查看非当前相位允许,动作禁用**:用户可以点其它页签查看,但页签上给当前真实相位一个标记(如小圆点),且非当前相位下所有动作按钮(锁定作战单/做多/做空/确认)一律 `disabled` + 灰态,hover 提示"非当前阶段"。判定依据是**真实相位**,不是页签选中态。
3. **复盘 widget 未收盘态**:周期未收盘时点"盘后复盘",复盘 widget 不渲染十几行 `--`,渲染一句"本周期未收盘 · 复盘数据收盘后生成"。归因/判卷/台账三个分节整体替换成这一句,不要一节一节地空转。
4. 本任务只做**禁用与状态显示**;按钮点击后的真实行为(正确 payload、错误反馈)属于 10b,不要顺手做。

## A6 · standard-kline 重复 id 修复(P4,包内)

**证据**:页面上 `tv-attr-logo` id 出现 6 次、`a` id 出现 6 次(每个图表实例注入相同 id),HTML 无效。修法:包内改为 class(或实例化时加后缀),包版本递增,包内测试补一条"多实例无重复 id"断言。**包保持 provider-agnostic,不出现业务词**。

## 测试(先红后绿)

追加到 `tests/test_dashboard_dualtrack_split_static.py`(新建测试类或函数,不改 09 的现有断言):
1. 页面源码不含 `09a`/`09b` 字样。
2. 页面源码不含 `machine_fills_hidden` 字样。
3. chartrow 新布局:大级别图容器在主图下方(断言新布局的结构类名存在、旧的 `7fr 3fr` 网格声明不存在)。
4. 相位推导函数存在且默认页签绑定真实相位(断言函数名 + 调用点)。
5. 复盘 widget 有未收盘占位文案("本周期未收盘")的渲染分支。
6. 空仓语义:持仓/风险 widget 渲染路径有"无持仓"分支。
7. 包内测试:多实例无重复 id。
8. 全仓 `python3 -m pytest -q` 全绿;`node --test packages/standard-kline/` 全绿。

## 验收(浏览器,真实数据)

- `.inbtns` 与工具条按钮 `getBoundingClientRect` 相交为 0(1280/1440/1600 三档)。
- 1280px 视口保持双画布两列;主图宽度 ≥ 画布宽的 90%;大级别图在主图下方横排、画图区不再被工具条吃掉一半。
- 机器轨盘中 kline 收缩为盲态条;post 页签(未收盘时)看到"本周期未收盘"而不是 `--` 矩阵。
- 盘中状态下"盘前"页签里锁定按钮是 disabled 灰态。
- 状态 widget 无原始英文 key、无孤词"warn"。
- 空仓时持仓/风险 widget 显示"无持仓"。
- 全部相位 × 两画布逐屏截图附 PR;console error = 0。

## DOD

- [ ] A1-A6 全部完成,证据截图逐项附 PR
- [ ] 新增测试 + 全仓 + 包测试 + token 白名单全绿
- [ ] v5/R4 零 diff;decision-log 追加末尾
- [ ] 未决事项列出(至少:全站品牌名统一)
